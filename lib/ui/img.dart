import 'package:flutter/material.dart';

class Img extends StatelessWidget {
  const Img(this.src, {super.key});
  final dynamic src;

  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    throw UnimplementedError();
  }
}
